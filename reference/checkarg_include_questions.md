# Check that include_questions uses the QID format, and format for API call:

Check that include_questions uses the QID format, and format for API
call:

## Usage

``` r
checkarg_include_questions(include_questions)
```
