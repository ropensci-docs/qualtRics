# Construct a header to send to Qualtrics API

Construct a header to send to Qualtrics API

## Usage

``` r
construct_header(API_TOKEN)
```

## Arguments

- API_TOKEN:

  API token. Available in your Qualtrics account (see:
  <https://api.qualtrics.com/>)
