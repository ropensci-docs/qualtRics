# Check if httr GET result contains a warning

Check if httr GET result contains a warning

## Usage

``` r
check_for_warnings(resp)
```

## Arguments

- resp:

  object returned by
  [`qualtrics_response_codes()`](https://docs.ropensci.org/qualtRics/reference/qualtrics_response_codes.md)
